const fs = require('fs'); const content = fs.readFileSync('src/tools/PdfCrop.jsx', 'utf8'); const lines = content.split('\n'); let depth = 0; for(let i=0; i<lines.length; i++){ let line = lines[i]; if(line.includes('//')) line = line.split('//')[0]; let op = (line.match(/<div[ >]/g) || []).length; let cl = (line.match(/<\/div>/g) || []).length; if(op > 0 || cl > 0) { depth += op - cl; if (depth < 0) console.log('NEGATIVE DEPTH at line ' + (i+1)); } } console.log('Final depth:', depth);
